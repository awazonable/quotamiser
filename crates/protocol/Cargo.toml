[package]
name = "quotamiser-protocol"
version = "0.1.0"
description = "Wire formats for QuotaMiser: SSE framing and OpenAI error bodies. No I/O."
edition.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
serde_json = "1.0"
thiserror = "2.0.20"
