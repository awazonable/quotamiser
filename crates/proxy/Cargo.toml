[package]
name = "quotamiser-proxy"
version = "0.1.0"
description = "The QuotaMiser HTTP proxy: ingress, dispatch, supervision and settlement."
edition.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
